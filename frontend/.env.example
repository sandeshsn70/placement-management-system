# Base URL of the Spring Boot API.
# Leave unset to use the Vite dev proxy (/api -> http://localhost:8080).
# Set this when the backend runs on a different host/port or in production.
VITE_API_BASE_URL=
